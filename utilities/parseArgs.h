#ifndef _PARSEARGS_H
#define _PARSEARGS_H

#include <stdio.h>
#include <string>
#include <string.h>
#include <vector>
#include <algorithm>
#include <cctype>

using std::vector;
using std::string;

vector<string> parseArgQids(const char* arg);

#endif
